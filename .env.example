# Optional. Raises the WD Tagger Space's request priority.
HF_TOKEN=

# Optional. Enables the daily per-IP identify rate limit (Upstash Redis, free tier works).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
